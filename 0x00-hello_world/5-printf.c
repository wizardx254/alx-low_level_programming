#include <stdio.h>

/**
 * prints " with proper grammar,
 * but the outcome is a piece of art"
 * Return: 0 successful, non-zero otherwise
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
